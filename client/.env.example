# Base URL of the backend proxy (the Express web service).
# Local dev: leave blank to use the Vite dev proxy (/api -> localhost:8787).
# Production (Render Static Site): set to your backend web-service URL, e.g.
# VITE_API_BASE_URL=https://agri-weather-server.onrender.com
VITE_API_BASE_URL=
